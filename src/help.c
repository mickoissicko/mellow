#include <stdio.h>
#include <stdlib.h>

void Help()
{
    printf("Currently, you must only use flags, or command-line arguments in order to execute commands, etc.\n");
    printf("You can refer to the wiki at: https://mick.gdn/wiki/mmgr for detail\n");

    exit(1);
}

